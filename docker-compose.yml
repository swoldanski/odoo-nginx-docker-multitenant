version: '2'

services:

  web:
    image: nginx
    ports:
     - "80:80"
    depends_on:
     - odoo
    
  db:
    image: postgres
    volumes:
      - data:/var/lib/postgresql/data
    
  odoo:
    image: odoo
    depends_on:
      - db

volumes:
  data:
    driver: local
